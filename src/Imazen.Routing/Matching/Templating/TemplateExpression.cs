namespace Imazen.Routing.Matching.Templating;

public record TemplateExpression
{
    // string variable name
    // one or more modifiers, like 'default(value)' or 'optional:?' or 'lower' or 'upper' or 'trim' or 
}